<h1>all of the tasks</h1>

<!-- <ol>
   @tasks.each do |e|
  <li>
    <ul>
     e.each do |i, j|
      <li>"#{ i }: #{ j }"</li>
     end
    </ul>
  </li>
  <br>
 end
</ol> -->

<ol>
  <% @tasks.each do |t| %>
    <p>
      <li><strong><%= "#{ t.name.capitalize }: " %></strong><%= link_to "Details here", task_path(t) %>
      <%= link_to "Edit task", task_edit_path(t) %></li>
      <%= link_to "Delete task", task_delete_path(t) %></li>
    </p>
    <% end %>
</ol>

<%= link_to "New Task", task_new_path %>
