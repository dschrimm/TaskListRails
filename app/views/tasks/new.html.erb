<h1>Submit a New Task</h1>

<%= form_for @task do |f| %>
  <p><%= f.label :name %>
  <%= f.text_field :name %></p>

  <p><%= f.label :description %>
  <%= f.text_area :description %></p>

  <p>
    <%= f.label :completed_at %>
    <%= f.date_field :completed_at %> # i would like to learn to restrict this to be <= current date.
  </p>

  <p>
    <%= f.submit "Submit" %>
  </p>
<% end %>
